# Base tooling for documentation, code inspection, and future experiments.
# No runtime implementation dependencies are required at the current stage.

numpy>=1.26
pandas>=2.2
pyyaml>=6.0
tqdm>=4.66
rich>=13.7
matplotlib>=3.8
psutil>=5.9
tabulate>=0.9
